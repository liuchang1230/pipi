# 领域模型 / Domain Glossary

本文件记录本项目的领域词汇，供架构评审（improve-codebase-architecture）与设计讨论使用统一语言。

## 术语

- **会话（Session）**：pi 在 `~/.pi/agent/sessions/<encoded-cwd>/` 下以 `.jsonl` 存储的一段对话。侧边栏展示其元数据：首条用户消息（预览）、消息数、显示名、mtime。
- **远端数据目录（AgentDir）**：远程 pi 的数据根目录，默认 `~/.pi/agent`。远程连接可填 `agentDir` 覆盖（经 `PI_CODING_AGENT_DIR` 环境变量生效，`~` 前缀/绝对路径，只允许安全字符）；用于多人共用 SSH 账号时隔离会话/模型/扩展。SFTP 侧 `remoteAgentDir(remote, homeDir)` 与 shell 侧展开保持一致。
- **会话索引（SessionIndex）**：主进程中唯一负责"列出某 cwd 的会话列表"的深模块。接口：`cached(target, cwd)`（TTL 守卫的同步读取）、`refresh(target, cwd)`（异步、快照增量、协作式分块解析）、`startPolling/stopPolling(target, cwd)`（4s 轮询）、`onChange/onAnyChange`（变更订阅）。target 是判别联合 `{kind:"local"} | {kind:"wsl", distro}`（2026-09-07 深化：WSL 从绕 seam 的旁路收编为 seam 下真正的第二份 backend adapter；SFTP 远程走独立的 remote cache + hydration 管道，不是此 seam）。WSL home 解析经 `setWslHomeResolver` 注入（生产 getWslHomeAsync，异步 spawn，消灭点击路径 spawnSync 阻塞隐患），路径映射经 `setWslPathMapperForTests` 注入（生产 \\wsl$ UNC），测试不依赖 wsl.exe。
- **激活事件（tabs:active）**：主进程 → 渲染层的原子激活载荷，携带 `{id, cwd, isRemote, sessions?}`。渲染层用一次 `applyActive` 应用，不再级联多次 setState。缓存命中的 `sessions` 让点击路径省掉一次 `session:list` 往返。
- **面板状态 store（Pane Store）**：渲染层按面板拆分的 zustand store：`useTabsStore`（标签与激活上下文）、`useSessionsStore`（项目目录 + 会话列表 + 水合 + 批量选择 + 会话生命周期 action）、`useTreeStore`（文件树）、`useViewerStore`（查看器 + 跟随 + `openFile` action）、`useLayoutStore`（跨 pane 几何：左右栏宽度、查看器折叠）、`useUiStore`（全局瞬时 toast）。面板组件用选择器订阅，互不牵连。
- **面板容器（Pane）**：渲染层按面板拆分的容器组件：`SidebarPane` / `TerminalPane` / `ViewerPane`。每个容器只订阅自己 slice 的选择器；跨 pane 动作（开会话、开文件、存文件刷树）走 store action，不经过 App。App 只留组合壳 + 事件编排（激活处理、主题、对话框）。
- **会话生命周期 action**：`sessionsStore` 的 `openSession / openRemoteSession / deleteSession / batchDelete`。删除后刷新本地项目缓存的规则（“已删会话不得残留在侧边栏”）与删除本身同住一个模块。
- **项目浏览 action（Project Explorer Action）**：`sessionsStore` 的 `toggleProject / deleteProject / newProjectSession`。水合阶段编排（本地预览 vs 远程/WSL 展开、缓存快路径、远程会话优先/暂停、treeOrigin 记录）全部收进 store，pane 只调一个 action——不再有 17 个原始 setter 穿过 interface。
- **对话框模块（Dialog）**：`ModelConfigDialog` / `RemoteDialog` / `RemoteDirPicker`。每个对话框自带全部表单状态与处理逻辑（挂载即全新），interface 只有 `onClose`；初始目标（跟随活动页签）在惰性 useState 里同步计算，首帧即正确。
- **PTY 流（TabStream）**：主进程按标签合并 pty 输出的流 adapter（5ms flush / 64KB 上限），避免每条 chunk 一次 IPC 消息。
- **远程服务器节点（RemoteServerGroup）**：侧边栏「远程服务器」分区的连接节点（与 WSL 发行版节点对称）。由 remote-history + 远程项目 + 打开标签三源合并去重（键 `user@host:port[agentDir]`，与主进程 remoteKey 同构）；`connected` 取决于是否存在同键标签，激活目标优先连接 shell 标签（`· 连接`）。分组逻辑是纯函数 `groupRemoteServers`（remote-servers.ts，可单测）。

## 关键架构决策

- 点击会话 → 中间页显示的路径上，主进程不允许出现同步全量 JSONL 解析（会阻塞全部 IPC，包括终端流）。一切会话列表读取必须走 SessionIndex（异步 + 缓存 + 增量）。
- 点击路径上主进程不允许出现同步子进程 spawn（`where.exe` / `node --version` / `pi --version` 各阻塞 0.03-1.2s）：检测结果（pi 路径 / node 是否安装 / pi 是否可用）必须在启动时预热并缓存（`warmPiDetection` / 检测缓存），会话文件标题只做首尾 64KB 范围读取。
- 渲染层任何跨面板数据必须放在 store，不得重新放回 App 的本地 useState；App 保持为组合壳 + 事件编排。
- 终端输出（pty → 渲染层）必须合并写；输入路径（渲染层 → pty）不合并。
- 跨 pane 动作必须收进 store action（`viewerStore.openFile` / `sessionsStore.openSession` 族 / `tabsStore.createTab` 族），容器不许把动作重新放回 App 回调；App 的 selector 订阅只覆盖对话框/编排真正读的 slice。
- 对话框是自含模块：状态不回流到 App，App 只持打开标志；store 的 `set` 一律用函数式更新，循环内禁止用一次性快照（会互相覆盖）。
- **默认视图 = 终端（pty TUI）**（2026-08-11）：新 tab（本地/WSL/远程）一律 `createTab` 跑 pi TUI；聊天视图是可选功能（TabBar「聊天视图」），本地切 SDK 进程内后端、远程/WSL 切 `pi --mode rpc`，同 tab id 双向切换（`switchTerminalToSdk`/`switchSdkToTerminal`/`switchTerminalToRpc`/`switchRpcToTerminal`）。本地 pty tab 需要系统 pi 二进制（`ensurePiReady`）；SDK worker 启动时后台预热，切聊天零等待。
- **ConPTY 冻结自愈（stall watchdog + 电源重建）**（2026-08-14）：Windows 锁屏/休眠后 conhost 管道可能停摆（输出冻结、输入被吞，OS 级已知 bug，VS Code/Windows Terminal 同款，均靠重建终端恢复）。两层机制：① `writeTab` 对 pi tab 在 ≥3min 零输出后的首次输入布设 8s 回显探针，无回显 → `restartTab`（同 id 原位重建 pty，保留 tab id/标题/会话，pi 从 JSONL 续接；shell tab 豁免，防止误杀静默长命令；系统 CSI 写入豁免）；② `powerMonitor` 锁屏/休眠 ≥10min 后 unlock/resume 时主动重建所有 pty tab。`onExit` 一律带 `tabs.get(id).pty !== term` 陈旧守卫，防旧 pty 退出事件破坏替换后的新记录。（阈值从 20min 降到 3min：探针自取消——健康 pty 输入即回显，降阈值零误杀成本；用户实测“闲置久后输入无反应”多为 5–15min 场景，20min 触发不了。）
- **自动跟随增量读（session-watcher，2026-08-14）**：watcher 负责从会话 JSONL 里提取 read/write/edit 工具事件驱动右侧文件跟随。读取路径改为**真增量 + 永不阻塞事件循环**：`readAppended` 异步定位读（fs.promises open + handle.read 带 position），单趟封顶 1MB；跨趟未完成的行由 `state.partial` 缓冲（`splitDelta` 纯函数），超 cap 单行不丢不重；`StringDecoder` 防 UTF-8 字符跨趟截断；drain 单飞（`draining` + `debouncedPending`，防并发读同一 delta 双发）；每趟快照 `st` 并在每个 await 后校验 `state === st`，切 tab 时在途 pass 安全中止不重放；活动文件选择 `pickActiveJsonl` 走缓存 stat + 2s TTL 全扫描；resume 种子由 `seeding` 标志保护，watch 事件不会把种子变成全量重放。事件契约 `{path, kind}` 不变。
- **懒加载文件树（lazy tree，2026-08-14）**：本地树从“深度 12 急切全递归 + 整树 IPC”改为**浅层懒加载**——`file:list`/`file:list-dir` 每次最多一次浅 readdir（目录 `children: undefined` 表示未加载、`[]` 表示真空，展开才取并不可变注入）；缓存按目录浅列（**root 感知键** `${root}\u0000${relDir}` 防双根碰撞，TTL 5s + generation + 共享 in-flight），mutation 失效父目录链；auto-follow 刷新 = 重列根 + 已展开目录（浅列）+ 1s 冷却钳高频写；`expandDir` 用 **per-dir 序号**（不共享 loadTree 的 treeReqSeq，独立目录互不取消，同目录后到者生效），origin/折叠/节点消失守卫防陈旧注入，失败折叠目录待重试；`injectChildren`/`findNode` 按全路径前缀匹配（节点路径各级均为根相对）。远程/WSL 惰性实现（浅列 + 导航）保持不变。
- **文件预览读闸（read gate，2026-08-14）**：文本预览上限 1MB（`TEXT_PREVIEW_MAX_BYTES`），超限**从不整读**——`readPreviewFromAbs`（本地/WSL 共用）先 stat 预检，超限走定位读（open + handle.read）取头尾各 512KB（`TEXT_PREVIEW_HALF_BYTES`），标记 `truncated: true`；SFTP 用 `client.get` 的 `readStreamOptions {start,end}` 只拉两段（v12 直通 ssh2 createReadStream）。超限二进制走 binary 分支；位图 1–10MB 仍整读换取完整 base64 载荷、>10MB 无载荷（与既有图片上限一致）。三路径共用 `isBinaryBuffer`（8KB 采样，NUL/非文本占比）判定。渲染层：截断文件**锁定只读**（保存会覆盖截断内容），显示“已截断”提示条 + 头尾内容；截断 SVG 回退文本分支。边界保真：`bytes` 始终为真实大小，头尾窗口在 size > cap 时数学上不重叠。
- **tab:create 先出终端（连接体感，2026-08-14）**：① 远端主题同步**后置为后台**（createTab 先出，getSftpLease + syncThemesViaSftp fire-and-forget，best-effort，TTL 门控不变）——死服务器不再让终端晚出现 ≤15s；② `tab:alive` 语义从“已注册”改为“**注册 + pty 进程存活**”（`isPtyTabAlive`：node-pty `exitCode === undefined`，windowsPtyAgent 运行时暴露）——ssh.exe 连接被拒/主机不可达会快速退出 → 对话框正确报失败，不再假“已连接”；RPC/SDK 外部 tab 无 pty → 保持注册语义；`waitUntilAlive` 首轮轮询前加 300ms settle（失败进程的退出事件 50–500ms 才派发，立即轮询会漏判）；③ 本地首次 pi 自动安装复用 `runLocalPiInstall`（in-flight 守卫 + begin/result 事件）→ PiInstallDialog 显示进度，不再 19s 无反馈。
- **体感打磨轮（2026-08-14）**：① 单实例锁（`requestSingleInstanceLock`，二次启动聚焦现有窗口；`whenReady` 移入锁的 else 分支）+ `uncaughtException` 兜底（setInterval/watch 回调同步 throw 不再炸主进程）+ `show:false`/`ready-to-show` 消除启动白闪（`did-fail-load` 兜底显示）；② alert→toast 统一（重命名失败、远程项目引导）；③ WSL 连接 in-flight 守卫（双击不再双开 tab，失败有 toast）；④ 聊天发送 300ms 双发守卫 + 聊天/终端视图切换 try/catch/finally（失败不再永久禁用按钮）；⑤ 模型配置对话框 4 个异步按钮（验证/删除/移植/保存）统一 busy 守卫（**校验在 guard 之前**——reviewer 抓到首点路径的 busyAction 卡死 bug）；store 层 3 处原生 confirm（会话/批量/项目删除）保持现状（样式化需要把确认逻辑从 store 提组件层，成本高收益低）。
- **终端激活聚焦（2026-08-15）**：TerminalView 挂载和 tab 激活时**从不调用 `term.focus()`**——xterm 的输入依赖辅助 textarea 聚焦，打开会话后未聚焦则打字完全无效（用户症状：“打开软件、打开会话输入没反应，必须多次切换会话”——切换时误触终端点击才恢复）。修复：TerminalHost 给 TerminalView 传 `active`，挂载时若激活立即 focus；`[active]` effect 在 tab 激活时 refocus + 绑定 window focus 监听（窗口回归时还给终端；监听放此 effect 内避免闭包捕获首次渲染的 active 值）。
- **聊天视图全量问卷对话框（questionnaire，2026-08-19）**：`ask_user_question` 由三方包 `@juicesharp/rpiv-ask-user-question` 提供。终端视图走 TUI 覆盖层（标签栏 + Shift+Tab/← 回退）；聊天视图（SDK worker 与远程/WSL RPC 都以 `mode: "rpc"` 绑定扩展）走 RPC 对话框走查器（rpc-fallback.ts）——**逐题弹一个 select/input 对话框，无法回退**（包文档明示的取舍）。修复为 App 端拦截：`ChatPane` 收到走查器第一个对话框时，若聊天流里有运行中的 `ask_user_question` 工具块（`chatStore` 存有完整 args）且标题前缀匹配 `[header] question`，则**按住首个对话框不答**，弹出 `QuestionnaireDialog`（标签 chips + ←/→ 回退 + Submit 汇总页 + 多选复选 + 旁侧 preview + 本地化 Type something. 行）；提交后按 `buildFlushSteps` 预排的发射顺序逐题应答走查器对话框（select 回传走查器原样选项行、custom 回传哨兵行使走查器开后续输入框、multi 回传 "1,3" 文本），取消则对挂起对话框回 `{cancelled:true}` 中止整卷。纯逻辑（解析/标题匹配/发射序/payload 构造）抽成导出纯函数并单测。关键守卫：① 键盘监听按 `active` 门控——隐藏 tab 的问卷绝不劫持可见 tab 按键；② input/IME 组合态内箭头键与 Esc 让位给输入框（Esc 在自定义输入内 = 退回选项，TUI 同款）；③ flush 中途收到不匹配对话框 → 回 cancelled 诚实中止（按序应答顺序错乱会污染题号映射）；④ 提交重入守卫（`flushRef.current` 已存在即返回）；⑤ 对话框真实卸载时自动取消挂起请求（防切换终端视图后 agent 永久卡等）；⑥ `extractSentinelLabel` 从首个对话框 options 末行取本地化哨兵标签。
- **多远程服务器可见（2026-08-20）**：SSH 服务器不再只是隐藏的连接标签——侧边栏「远程服务器」分区以服务器为节点、项目文件夹挂其下（与 WSL 分区对称，`RemoteServerSection`）；连接 shell 标签（startPi:false）**不再从标签栏过滤**（连接即可见、可切换，修复“连接成功但界面无反馈”）；目录选择器按服务器目标（`ensureRemoteConnection` 优先复用连接 shell 标签，失败有 toast + in-flight guard），不再永远指向第一个远程标签；未连接项目的点击在 `toggleProject` 内自动建连接标签（与 WSL 同款 in-flight guard + 失败回滚展开并置 error，避免永久“远程会话加载中”）。
- **远程连接状态诚实化（2026-08-20）**：进程存活 ≠ 连接成功（密码提示/握手挂起时 ssh.exe 仍活着）。连接 shell 标签（startPi:false）的远程命令在交出 shell 前 `echo __PIPI_READY__`（**纯文本行、零反斜杠转义**——`
` 经 ssh→bash 引用链在 Windows 会被吃反斜杠，实测 `printf '__PIPI_READY__
[2K'` 变成字面 `rx1b[2K`）；主进程用**行缓冲剔除**（`stripMarkerLine` 纯函数：只缓冲"标记前缀"的未换行尾，其余立即转发，标记行从流中吃掉、用户不可见）置 `tab.sshState="ready"`——唯一正向的"会话真实建立"信号。ssh 加 `-o ConnectTimeout=10` 快速失败；退出前未收到标记 → `failed`。`tab:conn-state` IPC + `waitConnState` 三态：ready/failed/timeout。对话框与节点连接**只在 ready 时提示"已连接"**，failed 提示失败（对话框停留可重试），timeout 提示"已发起连接，请在终端完成登录"。侧边栏节点四态：connected/connecting/failed/disconnected（pi 会话标签存在 = connected；激活目标 ready>connecting>failed 排序）。防僵尸：连接标签退出**先删 registry 再 emitTabsChanged**，复用路径过滤 `sshState==="failed"`。
- **连接着陆页（2026-08-20）**：连接成功后中间页**不直接是裸终端**——连接 shell 标签（`· 连接`）在 `sshState==="ready"` 时叠一层 `ConnectNotice` 提示页（"✓ 已连接到 xxx · 从左侧选择项目，或点 + 添加项目目录" + 打开服务器终端按钮）；**终端保持挂载在提示页下方**（点"打开服务器终端"只揭开覆盖层，历史不丢）。pending（连接中/等密码）无覆盖层，终端可直接交互输入密码。失败标签由主进程即时删除（toast/侧边栏报错），无残留失败页。**焦点守则**：覆盖层显示时 `TerminalView` 的 `blockFocus` 生效——切走再切回时把焦点给覆盖层按钮（否则焦点留在另一隐藏终端的 textarea，按键会不可见地执行到错误的 shell）。
- **删除服务器节点（2026-08-20）**：服务器节点行新增 ×（与 WSL 节点、项目行的删除入口一致），`sessionsStore.deleteServer` 级联：关闭该服务器全部标签 → 删除其项目条目（projects.json，不动服务器实际文件）→ `remote:delete-history` 按 (host,user,port,agentDir) 清历史 → 刷新项目与历史。删除"记录"而非"服务器本身"——节点由历史+项目+标签三源派生，三者都清才真正消失。
- **打开会话输入竞态修复（2026-08-20）**：中间面板打开会话后"有时无法输入、过一会恢复"的四个根因与处置：① ChatView 聚焦逻辑重写（`wasActive` 从"定时器前置位"改为"**聚焦尝试成功后置位**"，挂载 30ms 定时器 + 失败后 rAF 重试一次，清理时取消定时器与 rAF）；② ChatView 补 **window focus 恢复监听**（与 TerminalView 对齐——此前窗口失焦回归后聊天输入框不会自动夺回焦点，终端会）；③ **SDK worker 命令排队**（`sdk-queue.ts` 纯函数 `decideCmdRouting`：已注册且未关闭 → run；未注册但打开中 → queue；其余 → drop。`openingCmds` 按 tab 缓冲，`openTab` 在会话事件订阅建立**之后** FIFO 重放（先订阅后重放，prompt 流式事件不丢），打开取消/失败时对积压命令回 "Tab session not found"，队列上限 100 溢出回 "command queue overflow"）——修复"打开会话瞬间发首条消息被 'Tab session not found' 丢弃"；④ 未改：pty 侧 pi 启动期间输入延迟属 TUI 初始化固有行为（秒级自愈），远程 RPC 侧 `PassThrough` 缓冲本就把连接前输入延迟灌入。
- **会话树自愈 + 扩展远程分发（2026-08-21）**：① TreeDialog 空树三根因（新会话真空、远程/WSL pi 启动 15-20s 空窗、rpcSend 静默失败）处理：`treeStatus` loading/ready/error 状态机 + **3s 自动刷新轮询**（对话框打开期间持续，导航期暂停——导航轮询独占 get_tree；rpcSend 返回 false → 显式报错 + 重试按钮；挂载首请求丢失/worker 崩溃自愈）；空态按 chatStore messages 旁证区分"空会话"与"树为空异常"；`friendlyTreeError` 把 "Unknown command: get_tree" 映射为 pi ≥ 0.80.3 版本提示。② 扩展分发扩展层：`extension-sync.ts` 导出 `SHIPPED_EXTENSIONS` + `syncExtensionsViaSftp`（按文件内容比对，只传变更文件，每次密码连接都跑、不受主题 TTL 限制）；`tab:create` 远程密码分支合并进同一 SFTP lease，WSL 走 `syncWslExtensions`（`\wsl$` UNC 直写 + `getWslHomeAsync` 异步非阻塞取 home，与 `getWslHome` 共享缓存）——远程/WSL 的 pipi-tree-nav 扩展从此真正在目标机存在，不再出现"导航 prompt 落到普通 LLM turn"。上游 `pi --mode rpc` 无原生 `navigate_tree`（只有只读 get_tree/get_entries），RPC 导航的官方通道仍是扩展 `ctx.navigateTree` 桥。
- **模型一键配置模板（2026-08-21）**：`MODEL_PRESETS`（shared/model-presets.ts）覆盖国内合规渠道——DeepSeek（`https://api.deepseek.com/v1`，默认 `deepseek-v4-flash`，旧 chat/reasoner 已宣布停用）、硅基流动（`https://api.siliconflow.cn/v1`，必须带 /v1，默认 `deepseek-ai/DeepSeek-V3`）、Kimi（`https://api.moonshot.cn/v1`，默认 `kimi-k3`）、智谱（`https://open.bigmodel.cn/api/paas/v4`，默认 `glm-4.6`）。模板只填连接信息（name/baseUrl/provider/默认模型/常用列表），**API Key 一律用户粘贴（BYOK，app 不代持）**；自定义 OpenAI 兼容端点不需要模板——现有表单直接支持任意 baseUrl + openai-completions。ModelConfigDialog 顶部新增「快速模板」芯片行，点击即 `applyPreset` 填表并清编辑态。规格表补两条高置信条目（`qwen/qwen2.5-7b-instruct` 32K/8K、`glm-4-flash` 128K/4K，specForModel 按小写 id 匹配硅基流动前缀）。值均来自厂商官方文档核对。
- **key-based 免密远程扩展自动分发（2026-08-21）**：之前扩展只对密码连接远程分发（SFTP），免密服务器要手动放。现补 `syncKeyAuthExtensions`（index.ts）：连接（tab:create）后台 spawn `ssh.exe -o BatchMode=yes -o ConnectTimeout=10`，命令由 `buildSshInstallCommand`（extension-sync.ts 纯函数，可单测）生成——内容 base64 内嵌（echo <b64> | base64 -d > $HOME/.pi/agent/extensions/<file>），**全程无引号**（Windows spawn→ssh→远端 bash 免转义）。BatchMode=yes 让"实际要密码"的服务器快速失败不挂起；按服务器 key + SHIPPED 内容 sha256 做内存门控（app 升级内容变 → 重传，否则重连零开销）；agentDir override 跳过（需手动）；20s 超时兜底。至此三种目标全覆盖：密码远程 SFTP、免密远程 ssh、WSL UNC，时机统一为"连接后即装，下一次 pi 启动生效"。
- **会话树文件直读快路径（2026-08-21）**：远程 pi 未就绪/已死时 get_tree 不响应，TreeDialog 只能干等。新增 `tree:from-file` IPC（index.ts）：直接读会话 JSONL（本地磁盘 / 密码远程 SFTP / WSL UNC），用 `tree-from-file.ts` 纯函数解析建树（镜像 SessionManager.getTree 语义：parentId 建树、孤儿作根、label 解析、leaf=最后 entry；`parseTreeFileAsync` 协作式让出事件循环），**秒出树**；RPC get_tree 到达后以活数据覆盖（`rpcTreeArrivedRef` 防文件快照覆盖更新的活数据、`pendingNavRequestId` 防快照误触导航完成）。key-based 免密远程跳过文件直读（无 SFTP lease，避免 15s 连接超时挂起）→ 走 RPC。WSL 路径修复：`wslToWinPath` 迁到 wsl.ts 并幂等（已是 \wsl$\ 前缀直接透传，修复 WSL 双重 UNC 前缀 bug，全调用点受益）。loading 体验：1.5s 首响补发 + 等待秒数分阶段提示 + 45s 无响应降级为诊断错误（后台继续自动重试自愈）+「树来自文件快照」提示。
- **key-based 远程会话树文件直读（2026-08-21）**：免密远程此前被文件快路径跳过（无 SFTP lease），只能干等 RPC get_tree → 45s 降级文案。补 `sshCatRemoteFile`（index.ts）：复用 rpc-session.ts `sessionArg` 验证过的 base64 路径模式（`P="$(printf %s '<b64>' | base64 -d …)"; cat "$P"`，buildSshCatCommand 纯函数可单测）经 ssh.exe BatchMode 读会话文件——pi 死了也能读。三种目标全覆盖：本地直读 / 密码远程 SFTP / 免密远程 ssh cat / WSL UNC。
- **远程会话文件路径回填（2026-08-21）**：`createRpcTab` 原先用 `!remote && !wsl` 条件跳过 `linkTabSession`——"继续最近会话/新建"打开的远程/WSL 会话 `tab.sessionPath` 永远 undefined → tree:from-file 快路径失效（no session file）→ 只能干等 RPC get_tree。修：移除条件，远程/WSL 也回填 pi 报的 sessionFile（服务器路径正是 SFTP/ssh/UNC 文件读取需要的；`setSessionWatcher`/`sessionTitleFromFile` 对远程本就 no-op/try-catch，link 安全）。渲染层配套：TreeDialog 只在 from-file 返回 "no session file" 时每 15s 重试（pi boot 完成后路径才可链接；真实读失败不重复 SFTP/ssh 开销）。另查明 get_tree 卡顿的一个机制：rpc-mode 每条响应后 `await waitForRawStdoutBackpressure()`，stdout 输出背压（ssh2 通道拥塞/大数据量事件）会让命令循环整体停摆、后续 get_tree 排队——文件快路径正是绕开该机制的兜底。
- **会话文件自主定位兜底（2026-08-21）**：sessionPath 缺失（pi 命令循环卡死 → get_state 永不返回 → sessionFile 不回填，死循环）时，tree:from-file 现在自己找：`findRecentSessionFile` 按 tab 的 cwd 算 `<agentDir>/sessions/<encodeCwd(cwd)>/`（本地 sessionDirFor / 密码远程 SFTP list 按 modifyTime / WSL UNC），取最新 .jsonl 直读——**树显示完全不需要 pi 响应**。key-auth 免密远程跳过（要 ssh ls，暂缓）。渲染层慢提示附带文件读取失败原因辅助诊断。至此即使 RPC 命令循环永久停摆（rpc-mode 输出背压闸），分支树也能显示。
- **RPC 零输出看门狗（2026-08-21）**：密码远程"pi 正常但 get_tree 无响应"场景——若 ssh2 认证/exec/.bashrc 卡住，远程 pi 一个字节都不吐，命令循环当然永不响应。RpcSession 加 40s 零输出看门狗：spawn 后 40s 无任何 stdout 且未退出 → 发 `rpc_no_output` 事件（40s 覆盖远程 pi 15-20s 正常 boot）。TreeDialog 收到后（文件快照也没救时）显示明确原因（未装 pi/.bashrc 卡住/认证失败/通道未建立）；ChatPane 收到后置 bootTimedOut 横幅。
- **contextBridge 1000 层嵌套限制（2026-08-21 根因实锤）**：诊断日志（pipi-debug.log，全链路埋点：rpc-send 路由 / RpcSession SPAWNED·SEND·FIRST BYTES·RESP / tree:from-file / 渲染层事件入口 + 全局错误捕获）证明远程完全正常、主进程完全正常，断点在**渲染层收不到数据**——`fromFile REJECTED Electron contextBridge recursion depth exceeded (>1000)`。**嵌套会话树（长线性单链，如 758 条消息）跨 contextBridge 时深度爆炸被拒**（RPC get_tree 响应经 preload callback 同样被拒）。修复：**改传扁平 `get_entries`（entries 带 parentId）+ 共享 `buildTreeFromEntries`（shared/tree-build.ts，防环：沿父链上溯检测 A→B→A 环，环成员提升为根，绝不产生循环引用）在渲染层建树**；tree:from-file 同样返回扁平 entries。聊天（get_messages）一直正常佐证消息内容不超深、只有树结构嵌套超限。
- **远程 pi 对齐应用捆绑版本（协议契约，2026-08-29）**：远程/WSL 的 pi **不再追 npm latest**，而是与应用的捆绑版本（`package.json` 精确锁定 `@earendil-works/pi-coding-agent`）对齐——RPC 协议、扩展桥、事件解析都以捆绑版为准。检查 `checkRemotePiUpdate` 对比 `getBundledPiVersion()`（读 node_modules 里的包版本）；更新 `runRemotePiUpdate` 用 `buildRemoteAlignCommand`：`command -v pi` 探测安装方式（含 `/.bun/` → `bun install -g`，否则 `npm install -g`）装到**指定版本**，再尽力 `pi update --extensions`。**安装后必须验证**（重跑 `pi --version`）：新版 pi 的 undici 需要 `webidl.util.markAsUncloneable`，Node ≥20.10 有、**所有稳定版 Bun 都没有**（修复只在 bun main 分支）——装成功≠能跑，验证失败返回明确错误而非假成功。版本检测失败时 `friendlyRemoteProbeError` 把 stderr 归类为：运行时过旧（markAsUncloneable/TypeError）/未安装（command not found）/目录不存在/其余尾部；渲染层对远程 tab 的探测错误**每 tab 只 toast 一次**（`probeErrorShownRef`），不再静默失败。
- **RPC 退出诊断诚实化（2026-08-29）**：三个此前掩盖真实错误的点全修：① ssh2 的**真实退出码在 `stream.on('exit')` 而不是 `on('close')`**（close 无参数 → 永远 0）；`reportExit` once 守卫 + close 仅作兜底；② RPC 恢复命令尾部 `; true` 把任何失败吞成成功 → 移除；③ stderr 全程捕获（`lastStderr` 上限 4000 字符，退出时带最后 2000 字符随 `tab:rpc-exit` 事件下发，`EXIT` 行记入调试日志）。渲染层 `markExited` 用纯函数 `pickExitErrorLine` 取**最后一行非噪音**（过滤 `bash -i` 无 TTY 的 "cannot set terminal process group / no job control" 噪音），退出横幅显示真实错误而非 "Inappropriate ioctl" 误导。40s 零输出看门狗事件也带 stderr 尾部（`rpc_no_output`），启动超时横幅显示"远程输出：…"。纯逻辑（行选择/版本提取/错误归类/对齐命令构造）全部抽成导出纯函数并单测。
- **内置 pi 升级策略（2026-08-29）**：捆绑 pi 是"协议契约 + 兜底 + 远程对齐基准"，**精确锁定版本**（不用 `^`），随 app 发版升级、不单独更新。本地聊天（SDK worker）走捆绑版、本地终端走全局 pi（可 `pi update` 追最新）、远程对齐捆绑版。升级依赖时重点核对：RPC 命令/事件形状（get_state 响应字段等）、SDK API（createAgentSessionFromServices/SessionManager/DefaultPackageManager）、以及**运行时的 undici 依赖要求**（0.84.2+ bundle 引入 markAsUncloneable，限制 Node ≥20.10 且排除稳定版 Bun——Bun 服务器必须改用真 Node 跑 pi）。
- **内置 pi 版本受 Electron Node 约束（2026-08-29 关键发现）**：升级内置 pi 到 0.84.4 后本地聊天全挂——SDK worker 在 Electron（Node 20.19.1）加载即退出 1：**pi 0.84.3+ 的 `package-manager.js` 把 glob 实现从 `glob` 包换成 `import { globSync } from "node:fs"`，而 `fs.globSync` 是 Node ≥22.13 才有的 API**（0.84.1/0.84.2 用 `glob` 包，Node 20 正常）。本地探测脚本跑系统 Node 22 全通过，掩盖了这个问题——**必须用 Electron 真环境验证 SDK worker**（`node_modules/electron/dist/electron.exe` 起最小 main → worker_threads 加载 `out/main/sdk-worker.js` → open）。结论：**内置 pi 精确锁定 0.84.2**（兼容 Electron Node 20 的最后一个版本；0.84.3+ 需 Node 22，留待未来升级 Electron 后解锁）。远程对齐目标随之变为 0.84.2，服务器 0.84.4 → 会提示版本不一致并对齐到 0.84.2。升级 pi 前必须核对三项运行时要求：① node:fs 新 API（globSync 等，Node 版本门）；② undici webidl API（markAsUncloneable，Node ≥20.10 / 排除 Bun）；③ Electron ABI（node-pty 原生模块）。
- **远程对齐版本读取 bug：被本地全局 pi 影子化（2026-09-06 实锤）**：`getBundledPiVersion()` 原先经 `resolvePiPackageEntry()` 解析——该函数**优先本地全局 pi**（扩展检查需要跟踪 `pi update` 维护的那份安装）。用户本地 `pi update` 追到 npm latest（0.85.0）后，"捆绑版本"被影子成 0.85.0 → 远程对齐安装 `@earendil-works/pi-coding-agent@0.85.0`：既偏离应用 RPC 契约（捆绑 0.84.2），又在服务器 npm 源（镜像）未同步该版本时直接 `ETARGET notarget`（症状：远程安装失败 + Inappropriate ioctl 噪音前缀）。修：捆绑版本**只读 app 自带 node_modules 包版本**（`bundledPiPackagePath()`，主进程 fs 透读 app.asar；dev 即项目 node_modules），兜底解析 app 自身 package.json 的精确依赖 pin；**全局 pi 永不参与对齐基准**。另补 `stripShellNoise`（纯函数，单测）清掉 `bash -ic` 无 TTY 的 ioctl/job-control 两行噪音（它们在真实错误**上方**，尾选逻辑够不到），ETARGET 时附镜像同步滞后提示。
- **文件树右键"打开文件所在位置"（2026-09-06，WSL 后补）**：本地浏览（rootPath 预览或本地 tab cwd）与 WSL tab 的树节点右键菜单新增「打开文件所在位置」→ 新 IPC `file:reveal`（index.ts，`{tabId, rootPath, relPath}`）：本地路径走 `resolveWithin` 防越界（堵反斜杠 `..\..` 逃逸、绝对/UNC 参数）；WSL 节点路径是绝对 linux 路径，复用 `wslFullPath`（posix→`\\wsl$\distro\...` UNC + 自带越界守卫，与 WSL 文件读写同款）；SSH 不提供（远程磁盘无 Windows 位置）——渲染层按 `treeOrigin.isRemote && !tab.wsl` 隐藏菜单项（tab 的 `wsl` 对象主进程 listTabs 本就下发，仅补类型声明），主进程对 remote tab 同样拒绝。`shell.showItemInFolder` 资源管理器定位选中（文件/目录同语义，VS Code 同款）。
- **模型配置热同步（model-sync，2026-09-06）**：模型配置对话框保存 models.json/auth.json（本机直写或 SFTP 写远程）后，**正在运行的 pi 进程永远看不到**——上游只在启动时加载一次（sdk-worker 的 ModelRuntime 缓存 / 远程 pi 进程内 snapshot），连 pi 自己的 `/reload` 都刻意不刷新模型注册表（reload() 只重载扩展/skills/prompts，无 modelRuntime.refresh()）。修法 = 随 app 分发扩展 `pipi-model-sync`（extension-sync.ts 三通道：本地/WSL UNC/远程 SFTP+ssh）：命令内调 `ctx.modelRegistry.refresh({allowNetwork:false})`（pi 官方暴露的 models.json 重载接口，纯离线）。触发链：对话框 save/delete/transplant 成功 → `uiStore.markModelConfigSaved(target)`（带目标：local/WSL distro/remote profile）→ 各 ChatView 订阅 bump → **纯函数 `modelSyncAppliesTo`（model-sync.ts，单测 13 例）做目标过滤**（local 触达 SDK + 本地 rpc 聊天 tab（PIPI_SDK_BACKEND=0 回退路径）；remote 按 host/user/port/agentDir 规范化比对；wsl 按 distro）→ rpcRequest get_commands 确认命令存在（未知斜杠命令会漏成用户消息）→ rpcSend prompt `/pipi-model-sync`（带 id）。**完成信号 = prompt 响应帧本身**：上游 prompt() 对扩展命令先行分发且跳过 _runAgentPrompt，handler 结束即 preflightResult(true) → success(id,"prompt") 立即返回；扩展命令从不启动 agent run，故**不存在 agent_settled 可等**（第一版在此翻车：主进程又监听不了自己 webContents.send 的帧——Electron 事件只走 main→renderer——两处皆空转）。响应/15s 兜底任一到达即清 modelList/thinkingLevels 缓存并重发 get_available_models；listener+timer 挂 effect 作用域由 cleanup 统一回收。附带修复：tab:list 的 rpc 映射补 remoteAgentDir（与 emitTabs 对齐，防渲染层重载后 scope 匹配失效）。教训：① 扩展命令的"回执"是 prompt 响应帧，不是 agent_settled；② Electron 主进程内不能监听自己 send 的频道，握手逻辑必须放渲染层（或 RpcSession.onMessage 挂钩子）；③ 未知斜杠命令 = 普通用户消息入库，任何 prompt 通道先 get_commands 验证。
- **WSL 会话列表收进 SessionIndex 的 seam（2026-09-07）**：此前 SessionIndex 的 docstring 声称 WSL 是同一 seam 下的 adapter，实际 WSL 分支在每个 handler 里绕 seam 直调 `wslScanSessionDir`（自带平行快照缓存、无共享失效策略、`getWslHome` spawnSync 在点击路径可阻塞主进程 5s）。深化：① SessionIndex 接口全部带 `SessionTarget` 判别联合（local/wsl），缓存键 `${target键} ${cwd}`（两个发行版同名目录不碰撞）；WSL adapter 内部用 `getWslHomeAsync` + `\wsl$` UNC 映射，与本地共享同一套快照增量/协作解析/emit 实现；`wslScanSessionDir` + 两张快照 Map 删除。② 调用点收编：`session:list`/`session:list-remote` WSL 分支走 `cached/refresh`；4s 标题轮询的 WSL 分支删除（改为 emitActive 对活动 WSL tab `startSessionsPoll(wslTarget, cwd)`，与本地完全同构）；`onAnyChange` 转发层路由 WSL 列表到 `session:remote-updated` 通道（复用 `sameSessionPaths` 纯函数做文件集变化判别——只有文件集变化才重发列表，mtime 漂移只同步标题，保住"会话一直在刷新"的既有修复）。③ 事件归属：同发行版多标签时转发器用 `pickWslEventTab`（wsl-event-tab.ts 纯函数，轮询 scope 匹配 > 路径匹配 > 发行版兜底）选定 tabId——reviewer 抓到的回归：按插入序取第一个同发行版 tab 会把事件归到错误标签（remoteSessions 缓存按 tabId 分桶 + hydration idle 清除按 tabId 匹配，归因错误 = 副作用永久化）。④ `invalidateFile` 改为按 encodeCwd 回编匹配缓存键（decodeCwd 对 Windows 盘符不是 encodeCwd 的逆：`--D--work-proj--` 解码成 `D://work/proj`，原实现其实从未精确命中过本地缓存键，靠 4s 轮询兜底掩盖）；修复过程中抓到的正则 bug：`/^\/\/wsl$\//` 里的 `$` 是锚点不是字面量（`wsl$\/` 中 `$` 后还有字符永不匹配），必须 `\$`。⑤ 测试：session-index.test.ts（26 用例，两 target 的 cached/refresh/TTL 过期/in-flight 合并/invalidateFile UNC/变更发射/轮询）+ wsl-event-tab.test.ts（5 用例）——SessionIndex 首次拥有接口级测试面。教训：① 字面量正则里的 `$` 在 `wsl$/` 形态下静默变成锚点，node -e 手测时转义层会骗过你，必须用测试验证；② docstring 声称的架构要靠调用点核实，注释不会自动成真。
- **远程会话加载提速（2026-09-07）**：远程侧边栏卡顿的四个根因与处置：① **水合全文件下载**——`hydrateRemoteSessionsRange` 原来对每个会话 `client.get(full)` 拉整份 JSONL（多 MB 文件 × 高延迟链路 = 主要开销），128KB 截断发生在下载后；改为 range 读（`readStreamOptions {start,end}`，与 remoteReadFile 超限分支同款已验证通道）：头 128KB 永远读 + **尾 64KB 仅超限文件读**（pi 把 session_info 改名条目 append 到文件尾，不读尾则大会话改名后标题丢失，镜像 pty.ts 本地 head+tail 64KB 标题读）；messageCount 饱和于窗口内条数（"N 条"是装饰性字段，不值得全文件传输）。② **每次密码连接都全量同步扩展**——3 get + 3 put 正好发生在用户首次展开会话时；加内容 sha256 摘要门控（与 syncKeyAuthExtensions 同款）：app 升级内容变 → 重传，否则重连连 SFTP lease 都不创建。③ **水合解析同步阻塞**——`parseSessionText` 换协作式 `parseSessionTextAsync`（4×128KB JSON 解析不再一口气压主进程事件循环，终端流共用该循环）；批次 4→8（OpenSSH sftp-server 单连接串行处理请求，大批次只省每批的 lease 往返/IPC 发射/渲染层 map 抖动，不省线上传输时间；50 会话 12 批 → 6 批）。④ **首事件误清 hydration 提示**——第一个批次（头 5）事件就把"正在补全远程会话信息"清掉而后排仍显示"同步中"；事件载荷补 `hydratedCount/totalCount`（preload/global.d.ts 类型同步），渲染层仅在 count>=total 时清 idle（旧事件无计数字段时回退到全行已水合判定）。教训：① 超限截断截"下载后"还是"下载前"差一个数量级的传输量，任何 `client.get` 全文件读取都要先问 range 能不能替代；② 事件载荷缺进度字段会让渲染层只能猜（猜错就闪或粘），进度语义该跟着事件走。
- **远程空会话目录 = 空列表，不报错（2026-09-11）**：`listRemoteSessionDir` 对会话目录执行 `client.list(sessionDir)`，目录不存在时 ssh2-sftp-client 抛 `list: No such file`（SFTP status 2 / code 2）被当成错误下发，侧边栏显示「远程会话加载失败：list: No such file」——而本地 `SessionIndex.doRefresh` 对同一情况是 `if (!existsSync(dir)) return []`（空，不报错）。修：新增 `src/main/sftp-errors.ts` 纯函数 `isSftpMissingPathError`（认 status 2 / "2" / ENOENT / NO_SUCH_FILE 标记 / Error 消息里的 `No such file`；普通字符串不检查，权限/网络/断连仍是硬错误），`listRemoteSessionDir` 捕获后返回 `sessions: [], fileCount: 0` 并记一行 debug 日志。单测 8 例。教训：远程与本地同类读取必须同语义——「目录不存在」是常态（pi 惰性建目录），不是失败。
- **会话树轮询单飞（"卡死"主因，2026-09-11）**：TreeDialog 的 3s `get_entries` 轮询只挡了导航态，没有 in-flight 去重；而远程单次响应实测 **15–18s**（ssh2 + `pi --mode rpc` 串行命令循环 + stdout 背压闸）→ 每 3s 发、15s 回，请求雪崩（一次对话框打开累计 **12949 次**发送），把 pi 的命令循环灌满，prompt/get_state/get_messages 全部排队，用户看到的正是「点了没反应、鼠标转圈」。修：`tree-poll-guard.ts` 纯函数 `createEntriesSlot(stallMs)` 提供单一在飞槽位——刷新轮询与导航轮询（1s，summarize 可达 180s）共用同一槽位；任何 `get_entries` 响应释放槽位（**在导航过滤之前**释放，否则迟到的刷新响应会永久占槽）；30s（`ENTRIES_STALL_MS`，高于实测 18s）无响应视为丢包可重取，单帧丢失不会让轮询永久停摆。单测 4 例。教训：任何「定时轮询 RPC」都必须有在飞闸门，且间隔要跟最坏往返时间比，不能假设毫秒级。
- **远程/WSL 文件树实时刷新（定时轮询已展开目录，2026-09-11）**：实时刷新的唯一触发点是本地会话 JSONL 的 auto-follow watcher，而 `emitActive` 对远程/WSL 直接 `stopWatching()`，ViewerPane 也明确 `origin.isRemote || origin.rootPath` 就 return → **服务器上 pi 写的文件永远不会自动出现在侧边栏**（本地则受 auto-follow 开关门控）。修：ViewerPane 每 6s（`REMOTE_TREE_POLL_MS`，窗口可见时）调 `treeStore.pollRemote()`；该 action 只对远程/WSL origin 生效、复用 `lastTreeRefreshAt` 冷却，并以 `force/noCache = false` 走主进程 5s 远程文件 TTL 缓存去重（每 tick 每目录至多一次 SFTP/UNC 列举）。`expandDir` 增加第三参 `refreshLoaded`：重列已加载目录但不绕过 TTL，且**后台轮询失败不折叠**用户已展开的分支（手动展开失败仍折叠以便重试）。本地保持事件驱动（精确、零轮询）。注：只刷树，不做远程文件跟随（那需要远程会话 JSONL 增量读）。
- **子代理模型设置（2026-09-11）**：子代理（analyst/reviewer/scout）的模型解析顺序是 agent `.md` 的 `model:` → `PI_MODEL`/`PI_PROVIDER` → pi 默认模型；而 **pi 只把这两个变量写进 bash 工具的子进程 env，自身从不读取**，app 也从不设置 → 子代理实际落在 pi 默认模型（通常正是主模型），表现为"子代理和主 agent 用同一个模型"。修：settings.json 新增 `subagents: { provider?, model }`（缺席/normalize 为空 = 跟随主模型），ModelConfigDialog 顶部新增「子代理模型」下拉（本地 models 列表 + 跟随主模型，立即持久化）；`src/main/subagent-model.ts` 把它转成两条注入通道——① 直连 spawn 的 env（本地 pty `env`、本地 RPC 子进程 `ChildProcessTransport`、SDK worker 的 `open` 请求载荷：worker 是长生命周期且 `process.env` 是线程副本，故每次 open 时 clear+assign 到 worker 自己的 `process.env`，改设置无需重启 worker）；② WSL/SSH 的 shell 命令前缀（Windows env 不过 ssh/WSL 边界），值单引号转义（模型 id 合法含 `/`、`:`）。`subagent-model.ts` 对设置读取失败返回 null（读配置绝不允许阻断 spawn）。单测 9 例。效果对"之后启动的会话"生效（运行中的 pi 进程 env 已固定）。
- **WSL home 缓存预热（2026-09-11，缓解）**：`getWslHome` 仍是 `spawnSync("wsl.exe")`（5s 超时），而它经 `resolveWslPath` 挂在 WSL 的点击路径上（`tab:create` 的 WSL 分支、`file:list`/`file:list-dir` 的 WSL 分支、WSL 文件读写）——冷缓存时一次点击可阻塞主进程数秒（全 IPC + 终端流一起冻）。当前处置：`wsl:list-distros` 返回前为前 4 个发行版 fire-and-forget `getWslHomeAsync`（共享同一缓存），把冷启动从"点击时"挪到"列表时"（侧边栏与远程对话框都会先列发行版）。彻底解法是把所有调用点异步化（`resolveWslPath` 变 async 的连锁改动），代价大，暂缓。
- **切换标签不再重下整段历史（"切标签卡死"主因，2026-09-11）**：ChatView 的事件订阅+历史引导 effect 依赖里含 `active`，于是**每次标签激活都会重建订阅、并再发一次 `get_messages` 拉整段会话历史**。大远程会话（日志里见过 1068 条）意味着每次切换都要跨 IPC 传多 MB JSON、在渲染层重新解析并重建时间线——正是"切标签/打开会话时卡住、鼠标转圈"。修：`active` 从该 effect 移出依赖（改用 `activeRef` 供挂载时的 steeringMode 补问），effect 现在每个挂载只跑一次；挂载时仍无条件 `requestHistory()`（隐藏标签需要、终端视图切回聊天视图的重新挂载也需要），实时事件订阅在标签隐藏期间持续生效所以转写不会变旧，显式路径（`new_session`/`fork`/`clone`/导航/`app_phase ready`）仍各自重取历史。教训：`active`/可见性这类"会频繁翻转"的 prop 不该进"订阅+一次性引导"effect 的依赖；需要当前值时用 ref。
- **连接不再是标签页：连接档案（profile）+ 探测（probe）+ 侧边栏状态点（2026-09-11）**：此前“点服务器 = 开一个 connection shell 标签”（`startPi:false` 的 ssh.exe pty），标签**既是连接本体又是登录输入面**，于是中间页总冒出一个带绿点、永不消失的标签（绿点 = `stripMarkerLine` 剥掉 `__PIPI_READY__` 后置的 `sshState:"ready"`）；而且 `file:*`/`session:*` 全部以 tabId 为句柄（`getTab(tabId).remote`）、`groupRemoteServers` 又从标签推导状态——想去掉标签就必须先换句柄。修：① **连接档案 = 地址身份**：`TargetRef = string | {tabId?} | {remote?} | {wsl?}`，main 侧 `resolveTarget` 把显式 profile 物化成**虚拟 TabInfo**（remote/wsl/remoteBrowsePath 字段照旧），所有远程/WSL 分支零改动接受“没有标签的连接”；SFTP 本就按 `stableRemoteKey` 池化 lease，profile 足够（`withSftp`）。② **连接 = 探测**：新 IPC `remote:probe`（ssh2 一次认证往返，无 shell 无 pty；有密码用密码，否则 ssh-agent + `~/.ssh/id_{ed25519,ecdsa,rsa}`），三态 `ready | need-password | failed`；渲染层 `remoteStore` 按 profile 存状态，侧边栏**地址旁的小圆点**（绿/红/脉冲/待登录）读它。③ **密码弹窗**：need-password → `RemotePasswordDialog`（密码 + 可选「记住」→ `remote:save-history` 写 remote-history.json，下次 probe 带密码静默连上）。④ **中间页不再被打扰**：点服务器节点只探测（不开标签）；点项目行走 profile（`projectTarget`）加载会话与文件树，**不再 `tab:activate`**；只有显式点「远程终端」按钮才建标签（那时用户要的就是终端，也是唯一能手动输密码的落点）；`RemoteDialog` 的“远程连接”也改为探测，三条入口都不再隐式建标签。⑤ **缓存键从 tabId 迁到可读 profile key**：main 的 `session:remote-updated` 增发 `remoteKey`（**必须是 `pty.buildRemoteKey` 的可读串，不是内部 sha1 `stableRemoteKey`**），渲染层 `remoteSessionCacheKey(remoteKey ?? tabId, cwd, agentDir)` + `TreeOrigin.remote/wsl` + `CurrentFile.remote/wsl`（保存回原连接），后台水合改从 `remoteProfiles` 注册表（`getSftpLease` / `resolveTarget` 为写入点）取 RemoteOpts，因此没有标签也能水合。`remote-servers` 的节点状态改为“探测注册表优先，活标签可把陈旧的 failed 升回 connected”。WSL 保持标签（`wsl.exe` 是传输本体、无密码），`port === 0` 即 WSL 的既有约定延续。教训：① 句柄（tabId）与身份（profile）一旦混同，UI 想改就得先换句柄——先加双模入口（`resolveTarget` 优先真标签、否则虚拟目标）比一次性重写安全；② 事件载荷里放“内部哈希键”是陷阱，跨进程的 key 必须是双方都能算出的规范形式（可读 profile 串），否则消费者永远匹配不上，症状是“永远同步中”。
- **“绿灯但一直加载”根因：探测与 SFTP 的认证方式不一致（2026-09-11，实锤修复）**：`remote:probe` 用「密码 ‖ ssh-agent + `~/.ssh/id_{ed25519,ecdsa,rsa}`」，而 `getSftpLease` **只用 `password`**——服务器认密钥时探测返回 `ready` → 侧边栏**绿点且不弹密码框**，随后所有 SFTP 调用（会话列表水合、文件树）认证失败 → 会话行永远停在“正在加载会话信息…”。修：① `remoteAuthOptions(remote)` 提成**模块级单一来源**（放 `getSftpLease` 旁，不能放 `whenReady` 闭包里——lease 在模块作用域），探测与 SFTP lease 共用同一份认证材料；② 探测的 `ready` 现在**必须通过 SFTP 通道验证**（`conn.sftp()` + `realpath(".")`，失败按 `已登录但 SFTP 不可用：…` 报 failed）——绿灯的语义是“数据通道可用”，不是“shell 能登录”；③ `toggleProject` 展开时**总是**走 `probeIfNeeded`（不再只在“没存密码”时才探测）→ 存了错密码也会弹登录框并回写原因；`REMOTE_PROBE_FRESH_MS = 60s` 内刚连上的档案免探测，重复点击零额外往返；④ 水合失败不再静默无限重试：错误分支把 `hydratedCount` 前推到目标值，把该条目移出 `scheduleRemoteHydrationWork` 队列（条目 TTL 过期 + 新列表从零水合，瞬时抖动仍能恢复），并打一行错误日志。测试：`remoteStore.test.ts` 增 `probeIfNeeded` 四例（新鲜跳过/过期重探/未连上从不跳过）；`sessionsStore.test.ts` 增“存了密码也要重探”一例，并在 `beforeEach` 重置 `remoteStore`（模块级 store 跨用例泄漏会让 `probeIfNeeded` 跳过探测、静默掩盖被测行为）。教训：① **连通性判断与数据通道必须共用同一套认证材料**，否则会产出最难查的一类 bug——状态说通、数据全错；② 探测的语义必须是“我能用它干活”，不是“我能登录”；③ 后台重试循环遇到确定性失败要主动出队，否则症状是“永远在加载”而不是“报错”。
- **连接状态以“数据通道结果”为准 + 熔断器 + 自动登录弹窗（2026-09-11）**：用户复现“服务器需要密码 → 却显示连接成功（绿）→ 会话看不到历史/一直正在加载”，并伴随“加载慢时整机卡”。根因有三层：① 状态是**推导**出来的（探测结果 / 标签存在 / sshState），不是**数据通道实测**结果——密钥能登录但 SFTP 不通时，界面照样绿；② 失败**没有退避**：认证失败后每个调用方各自重试（水合批次失败立即重新入队、4s 标题轮询、6s 文件树轮询），每次都是一轮完整 TCP+认证 → **连接风暴**，主进程事件循环（同时服务全部 UI IPC）被挤满，于是“加载慢 = 整机卡”；③ 失败**不上报**，渲染层只能显示“正在加载…”。修法（三层都从“推断”改成“实测 + 上报”）：`src/main/sftp-failure.ts` 纯函数（`isSshAuthError` / `isSftpPathError` / `describeConnectFailure`，12 例单测）；`getSftpLease` 加**按服务器（非按路径）的断路器**（`sftpFailures`：认证失败 60s / 传输失败 20s 内不发网络请求直接快速失败，成功或新密码立即清除）并在失败时 `emitRemoteStatus` 推送 `remote:status`；`remote:probe` 的 ready 现在**必须过 SFTP 通道**（`conn.sftp()` + `realpath(".")`），且探测与 SFTP 共用 `remoteAuthOptions`（密码 ‖ agent + `~/.ssh` 密钥），杜绝“绿灯但数据读不出来”；渲染层 `remoteStore` 新增 `applyStatusEvent` / `loginRequest` / `dismissLogin` / `submitLogin`，主进程发现的认证失败**自动弹登录框**（不管是谁先发现的），关掉后不再每轮轮询重弹（`dismissed`），连上后重新武装；`App` 里新增自含组件 `RemoteLoginDialogHost` 承载弹窗（App 保持零 store 订阅）；水合失败改为**出队**（`hydratedCount` 前推）不再无限重试。测试 38 例（remoteStore 16 / sftp-failure 12 / sessionsStore 等）。教训：① 状态必须来自“真实数据通道的一次实测”，用旁证（进程活、标签在、shell 能登）推断连接状态，最终一定会骗用户；② 失败重试必须有**退避 + 熔断**，否则失败的服务器的代价不是“慢”，而是“拖垮整个应用”；③ 判定失败后要**主动上报**给 UI 层，让 UI 有机会要凭据，而不是让读取悄悄失败成永久“加载中”。
- **关闭标签不再泄漏会话内存 + SDK worker 改懒启动（2026-09-11）**：实测主进程 RSS **920MB**（渲染 168MB / GPU 215MB，共 1.36GB）——大头在主进程。两个真实原因：① **ChatView 状态只在“切换视图”时清理，关闭标签从不清理**（`chatStore.clear` 只被视图切换调用）→ 每个开过的会话的完整 transcript + rAF 事件队列常驻，越用越多；修：`chatStore.retainTabs(liveTabIds)`（无变化时返回同一对象，避免每次 tabs:update 触发全量重渲染），由 App 的 `onTabsUpdate` 以主进程权威列表驱动，单测锁定。② **pi SDK worker 在启动 0ms 预热的**（`prewarmSdkWorker`），而它是 worker_thread、内存记在主进程头上，且加载捆绑 pi SDK（`node_modules/@earendil-works/pi-coding-agent` **139MB**）——却只有“本地标签切聊天视图”用得到，纯远程工作者完全白付。改懒启动：`prewarmSdkWorker` → `ensureSdkWorkerStarted`，只在 SDK 分支的 `tab:create`（首次真的要用）调用，代价是首次切聊天多等约 1s。另加 `[mem]` 内存趋势日志（startup / sdk-worker-start / 每 2 分钟 tick，落 pipi-debug.log），以后“很占内存”有数字而不是感觉。教训：① worker_thread 的内存是主进程的内存，预热即全局征税——只对部分用户有价值的重资源必须懒加载；② 任何按 tabId 索引的 store 都必须有“随标签关闭而回收”的路径，否则就是随使用时长单调增长。
- **“误判连接”根因三：标签存在 ≠ 已连接（2026-09-11）**：用户复现“点服务器→显示连接成功，切到终端视图才提示输密码”。链条：远程 pi 会话标签（RPC）在认证卡住时**标签照样存在**（`registerExternalTab` 先建、pi 后 boot），而侧边栏 `groupRemoteServers` 用 `hasSessionTabByKey`（“有没有会话标签”）当连接证据 → 绿点；实际认证失败 → RPC 通道卡死 → 用户切“终端视图”时 `rpcSwitchToTerminal` 起的 ssh.exe pty 才弹出密码提示。修：① 连接证据从“标签存在”换成“**标签证明了连通**”——TabInfo 增 `remoteReady`（`markTabRemoteReady`，仅在 RPC `get_state` 成功即 pi 真 boot 时置位），`serverStatus` 只认 `remoteReady || sshState==="ready"`；未 ready 的会话标签 = “connecting”（脉冲点），不再是绿。② RPC 认证失败**上报**：`Ssh2Transport` 错误回调新增 `onAuthFailure`（`isSshAuthError` 分类）→ `RpcSession.reportAuthFailure` → `emitRemoteState(remote, "disconnected", needPassword=true)`；key-auth 路径（BatchMode ssh.exe）在“零输出 + stderr 含 Permission denied”退出时同样上报；`get_state` 失败（非认证）报 `failed`（`authFailed` 标志防止后续通用失败覆盖更具体的“需要登录”）；`get_state` 成功 → `markTabRemoteReady` + `emitRemoteState(connected)`。至此“需要密码”不论从哪个入口（探测 / SFTP 断路器 / RPC 会话）都能把侧边栏点变黄并自动弹登录框。测试：remote-servers 改 3 例（会话标签需 `remoteReady`）+ 新增“未 boot 的会话标签 = connecting”回归例。教训：**“对象存在”永远不等于“对象可用”**——进程/标签/session 的存活是必要非充分条件，只有一次真实应答（marker 或 get_state）才是连通性证明；每一层状态派生都要问“我凭什么说它连上了”。
- **连接证据必须“随连接消亡而撤销”（2026-09-11 补）**：上一修把 `remoteReady` 作为连通证据，但它是**一次性置位、永不清除**——日志实锤：`get_state` 成功→绿，之后服务器 `对端关闭` 断开，侧边栏仍绿。修：`markTabRemoteDown(id)`（pty.ts）在 RPC transport 退出时清 `remoteReady`（shell 标签连 `sshState` 一起清）并重发 tabs；`rpc-session` 的 onExit 同时 `emitRemoteState(failed)` 把注册表也翻红；**用户主动关闭标签**先 unregister → `getTab(id)` 已空 → 不报 failed（否则正常关会话会把服务器涂红）。教训：**存活/就绪这类布尔证据是“租约”不是“档案”**——置位者必须同时拥有撤销者；否则 UI 状态只会朝“已连接”单向漂移，永不回落。
- **密码策略：非破坏性 + 显式 opt-in（2026-09-11 补2）**：修复"记住密码"时一度把密码存储改得**过狠**——`readProjects()` 读时清空 projects.json 所有密码、`saveRemoteHistory` 在无密码时删除旧凭据、手动删了 36.151.162.7 的历史密码——导致"之前能连的远程全部连不上"（实为密码被我删了）。回退后的正确策略：① 密码可存两处（remote-history.json 为服务器级、projects.json 为项目级历史遗留），**绝不读时/连接时删除**；② `saveRemoteHistory` 仅在 `entry.password` 非空时写入，否则保留旧值（非破坏）；③ `tab:create` 仍持久化服务器节点（host/user/port/path）但**剥离密码**；④ 只有 UI 显式勾选"记住密码"才写入密码（RemoteDialog/RemotePasswordDialog 默认 false）。教训：**"防止静默保存"的正确姿势是拦截"写"入口，不是删除"存"的数据**——数据是用户资产，清理必须显式、可逆、由用户发起；任何"读时迁移/清理并回写"的代码都是定时炸弹（App 一启动就永久抹数据）。
- **一次性登录的密码必须“活在内存里”（2026-09-11 补3）**：修"记住密码"后出现新循环——用户输入密码→探测成功→点会话又弹密码。根因：`probe()` 成功后只把 `status` 写入 `remoteStore.byKey`，**没存密码**；而后续 session/file 读取的目标密码来自 `groupRemoteServers` 的 seed（history/projects 里的**旧密码**）→ SFTP 用旧密码 → auth 失败 → 又弹框。修：① probe 成功把 `profile.password` 存进 `byKey[key].profile`（内存态，与"记住"（磁盘）正交）；② `groupRemoteServers` 用 `remoteStatus[key].profile.password` 覆盖 seed 的旧密码（服务器节点 + 项目行都覆盖）；③ `applyStatusEvent` 在非认证失败事件（connected/transport）时**保留**已验密码，仅在 `needPassword`（认证失败，证明凭据已失效）时丢弃。教训：**"记住密码"控制的是持久化，不是本次会话的可用性**——一次性凭据必须被后续所有读操作共享，否则每次成功登录后立刻被旧凭据打回原形。
- **历史加载超时短于真实往返 → 多 MB 重下风暴（"连接非常卡 + 未响应 + 占内存"主因，2026-09-11）**：用户报 36 服务器（`root@36.151.162.7:40008`，多 MB 会话）"连接非常卡、软件未响应、太占内存"。日志实锤：`get_messages` 单次往返 **17–45s**（08:32:20 / 08:32:49 / 08:33:36 / 08:34:21 四组），而 `ChatPane.requestHistory` 的 `rpcRequest(..., 15000)` 是 **15s**；`preload/rpcRequest` 一超时就 `removeListener`（迟到响应被永久丢弃）→ `chatStore.historyLoaded` 永远 false → `state_ready` 分支每来一次 `get_state` 就再发一次 `requestHistory()`，日志里 **08:32:03.062 / 08:32:03.071 / 08:32:04.923 三个多 MB 请求同时在飞**。后果链：SSH 链路被多 MB 载荷饱和（树/SFTP/终端全排队 = "非常卡"）+ 主进程反复解析多 MB JSON（"未响应"）；`[mem] tick rss=631MB heap=489/555MB` 正落在 08:26:08–08:26:53 的 get_messages 窗口内，内存"感觉很大"有了对应物。修：① `HISTORY_REQUEST_TIMEOUT_MS = 120_000`（≈实测最坏 45s 的 2.7×；这**不是**存活超时，存活由 12s 的 `PROBE_TIMEOUT_MS` 探针负责）；② 新增 `panes/history-gate.ts` 纯策略 `createHistoryGate`（`begin()` 占槽/拒绝并**记录**触发，`settle()` 释放并回答"是否需要补跑一次"），把 mount / `state_ready` / 分支导航三路触发合并成"至多一次补跑"，`settle()` 在 `finally` 里**先于任何 early return** 调用，保证超时也不会把闸门卡死；5 例单测。作用域前提已核对：每个 tab 只挂一个 `ChatView`（`TerminalHost` 全 tab 常驻、每 tab 一个实例），`historyRequestSeq` 只在持槽时自增，`historyLoaded` 不驱动任何转圈 UI，故 120s 不会卡出假加载。教训：① **超时值必须来自实测最坏往返，且要检查该超时的实现是否丢弃迟到响应**——若丢弃，一次"慢"就会被放大成"重下风暴"，把局部延迟变成全局卡顿；② "定时/事件触发的重活必须有在飞闸门"这条教训已经以 `tree-poll-guard`（get_entries 12949 次）出现过一次，这次又以 `get_messages` 出现——说明这不是某个对话框的疏忽，而是**凡是"事件触发 + 大载荷 + 慢链路"的组合都要先问一句"在飞时怎么办"**；③ 内存问题要先和传输窗口对齐时间轴再找对象，`[mem]` 尖峰与 `get_messages` 窗口重合直接把"占内存"指到了载荷上，而不是去猜泄漏。
- **定时重列必须做结构共享，否则等于全量重渲染（"当前项目文件一闪一闪"，2026-09-11）**：用户报文件树"树一直在，只是整列重新渲染/高亮闪一下"。根因是 6s 远程/WSL 轮询（`pollRemote`）每一轮都对 root + 每个已展开目录重新列举，服务端/主进程返回的是**全新解析出来的 FileNode 对象**（内容等价、引用全新），而渲染层三处都无条件换身份：① `loadTree` 直接 `set({ tree: sortedNodes })`；② `injectChildren` 即使子树未变也重建整条祖先链（`{...n, children}`）；③ `loadTree` 每次成功都 `new Set(s.expanded).add(first.path)`，即使该路径已在集合里。结果每个 `TreeBranch`/`TreeRow` 的 memo props 每 6s 全换 → 整列重渲染。附带一个更明显的闪烁源：`file:list` 的浅层 root 列表里目录的 `children` 是 `undefined`，直接替换会把已展开目录的已加载子树抹掉（行内显示"…加载中"），下一轮 `expandDir` 再补回来。修：新增纯函数 `mergeRelistedNodes`（结构共享）——长度不同（增/删）→取新列表；逐行比对 `path`/`type`/`name`，相同则**复用旧对象**；目录收到浅层重列（`children === undefined`）时**保留已加载子树**（children 归 `expandDir` 所有，不归父目录的重列）；子树未变→复用父节点；整体未变→**返回原数组**（`set` 后 selector 引用不变，React 直接 bail out）；`injectChildren` 同样在无变化时返回原数组；`expanded` 只在路径确实新增时才 `new Set`。`loadTree` 三个 `set({tree})` 点 + `expandDir` 注入点全部改走 merge。9 例单测，其中两例是 store 级身份断言（`expandDir(refreshLoaded=true)` 后 `tree` 引用不变、`loadTree` 后 `expanded` 引用不变），**改前必失败**。教训：① "定时重新拉一份数据然后整体替换"天然等于全量重渲染——memo 只能挡住"props 引用未变"，而重新解析出来的等价对象**永远是新引用**，所以"相等就复用旧对象"必须自己写，不能指望 React；② 要明确**子节点归谁所有**：父目录的重列不该触碰子节点，否则每次重列都会把用户的展开态闪一下；③ 先量再修——同一轮里我先把"日志同步写盘"排队（早期 6 万行/天确实致命），量完发现现在只有 1456 行/天，直接放弃，避免了无效改动。
- **提取 seam 时，注入端口的结构化类型必须覆盖“下游资源身份键”的全部字段（2026-09-11）**：把 `tree:from-file` 的四通道读（本地 / WSL UNC / SFTP / ssh）提成 `session-file-reader.ts` 时，为了“小端口”我把 remote 参数窄化成只含 `host/user/port/password/agentDir` 的结构类型（通道选择只需 `password`），并把对象**按引用**透传给 `withSftp`。当前没有 bug（按引用传，运行时不丢字段），但这是定时炸弹：`stableRemoteKey` 把 `host|user|port|**path**|agentDir` 一起哈希成 lease key，一旦后人把传入改成重建字面量（`{host: r.host, ...}`），`path` 丢失 → **同一台服务器算出两个 key → 两条连接 + 两套缓存**，而症状仅仅是“有时很慢”，几乎不可能查。修：把类型写成 `Pick<RemoteOpts, "host"|"user"|"port"|"path"|"agentDir"|"password">` 并注释说明**它不是“通道规则需要的字段”，而是“lease 身份 + 凭据”的投影**，`RemoteOpts` 一改这里就编译报错；另加一例断言 `readSftp` 收到的是**同一引用**（禁止重建）。教训：① “小接口”是指**入口少**，不是“参数字段少”——当参数会被透传给一个有自己身份语义的下游时，参数类型必须跟那个下游的身份对齐，否则窄化就是静默的资源池分裂；② 端口注入让模块可测，但**注入的对象仍然是下游的身份载体**，可测性不能靠缩小类型来换；③ 这类 bug 的症状（间歇性变慢）与原因（连接/缓存重复）距离极远，值得用类型系统提前拦住，而不是靠人记得。
- **历史改走会话文件（Stage 1–3，2026-09-11）**：远程历史加载占着 `pi --mode rpc` 的串行命令循环（实测往返 17–45s），于是 agent 看起来"卡住"。方案：文件（本地/UNC/SFTP/ssh）+ 镜像 pi 语义。四个可复用的结论：
  1. **pi 的 `get_messages` 不是"完整分支"，是 compaction 之后的窗口**。链路：`rpc-mode.js:536` → `agent-session.js:681`（`session.messages = agent.state.messages`）→ 压缩后 `:1542` 把它设为 `buildSessionContext().messages`。实测：一个 6.96MB 会话文件里 2944 条 message，但 `get_messages` **只返回 452 条**；我最初实现的"完整分支"版本会多发 **6 倍**（含已压缩掉的 228 条废弃分支消息）。教训：**“对端会给我什么”必须去对端源码里看，不能从数据结构推断**（文件是 append-only 日志，不等于“当前上下文”）。
  2. **镜像 pi 但用“测试期预言机”保真，而不是 import pi**：pi 的 exports map 不暴露内部路径，根入口又把整个 SDK 图（100MB+）载进主进程（正是要避开的内存开销），且 `session-list.ts` 已确立“自己解析 JSONL 不依赖 SDK”。所以逐行镜像 + 用 pi 真实 `buildSessionContext` 做**差分测试**（合成 fixture，确定性）。实测 3 个真实文件逐条相等（452/697/671）。
  3. **一致性可以“证明”而不靠猜**：`get_state` 返回 `messageCount = session.messages.length`，正是 `get_messages` 的长度 → 用“文件推导长度 == 该值”就能检测文件落后于 pi 内存态（未落盘导航/刚切会话/刚压缩），不等则回退 RPC。代价是一次小响应往返而非多 MB。**副产品**：若将镜像将来与 pi 漂移，长度不等 → 自动回退 RPC → 失败**安全**（慢但正确）。
  4. **主进程内部探针的响应 id 必须带前缀，渲染层丢弃**：否则内部 `get_state` 响应会进入渲染层 `state_ready` → 再入 `requestHistory` → 再发探针 → **自激回路**。同类陷阱：任何“主/渲染双方都能发同一命令”的协议，都需要区分“这是谁的请求”。
  另外两个顺带修的真 bug：① **四处裸 `rpcSend(get_messages)` 的响应被丢弃**（`chatStore.applyEvent` 根本没有 `get_messages` 分支，历史只经 `initMessages`）—— `fork`/`/new`/`/clone`/一个“active tab 无消息就补一次”的 effect，后者正是日志里每 ~30s 一条裸 `SEND get_messages` 的来源（历史因 15s 超时从未加载成功 → 每次重挂载都再发一次多 MB 的废弃传输）。改成 `requestHistory()`（代码自己的注释在 `onNavigated` 已声明这才是唯一真刷新路径）。
  教训：① **“读对端的大块数据”要先问三件事：它到底返回什么语义、字节数真的更小吗、会不会阻塞对端自己的命令循环**——我们量完发现首次读文件字节数**不比** `get_messages` 少（载荷≈文件大小，84–99%），真正收益是“不占 RPC 通道 + 可增量 + pi 死时也能读”，所以必须把预期降到“不再阻塞 agent”而不是“打开更快”；② **局部读（窗口/范围）在“位置顺序 ≠ 逻辑顺序”的数据上无法证明充分性**，这种优化宁可推迟也不能带静默错的余地；③ 行尾半行、叶子不是 message 条目、条目类型远多于“消息”——这些都是**真实数据**教我的，不是文档。
- **“远程项目打开很慢 / 列表显示后又被换成加载中 / 是不是在加载所有项目”（2026-09-11 诊断）**：用户三个抱怨各自定位到具体代码：
  1. **“加载我未打开的项目”是真的**：`scheduleRemoteHydrationWork` 遍历**所有**未暂停且未水合完的缓存条目（2 并发），而 `setRemoteHydrationPaused` 只暂停被收起的那个。排序里 `aNeedsHead`（未做完“头 5 条”那一轮）**优先于 priority**，所以另一个项目的头轮会排在当前项目后续批次**之前**抢走槽位。而 `markRemoteSessionPriority` 只降优先不暂停其他人。修：新增 `focusRemoteSessionHydration(key)`，在 `session:prioritize-remote` 里把**其他所有条目**置 `hydrationPaused = true`——即用户说的“只加载我打开的那个项目”；重复聚焦自愈（`markRemoteSessionPriority` 会清掉匹配键的暂停）。
  2. **“列表显示后又变加载中”有两个真原因**：① **缓存键不一致**——`sessionsStore.toggleProject` 写 `remoteSessions[remoteSessionCacheKey(tabId ?? project.remoteKey, …)]`（tab 存在时用 tabId），`openRemoteSession` 写 remoteKey，而 `groupRemoteServers` 只按 **`tab.id`** 查 → 单键查找静默落空 → 已取到的会话列表变空 → 回落到“加载中…”。修：两种键都试（优先 `projectSessions[project.id]`，再 tab 键，再 profile 键，且**跳过空数组**），4 例单测锁定。② **占位符替换了非空列表**——`SidebarPane` 的条件链里 `projectLoading[key] ? “加载中…”` 排在 `sessions.length === 0` 之前，所以**每次重新展开/刷新都会把已经渲染好的列表整体换成“加载中…”**。修改条件链：**非空列表永远不被占位符替换**，占位符只服务空列表（错误/加载中/无会话）。
  3. **首次列会话慢是固有的**：标签/首条消息来自**每个会话的 SFTP 头 128KB（+超限时尾 64KB）**读，批次 8、2 并发——慢链路上这就是主要开销（不是列表本身）。所以真正可做的就是 1（别把链路花在别的项目上）；想再快必须走“头 N 条 + 尾 M 条“渐进或分页（另议）。
  诊断手法：**日志里根本没有远程会话列表/水合的埋点**（只有 probe/title/rpc/transcript/mem/tree），所以无法从日志分“是列了所有项目”还是“只是慢”。已补 `[remote] list-remote <host> <dir>` 与 `[remote] hydrate batch <dir> n/N prio=` 与 `[remote] hydrate focus …`，**下次复现可直接从日志看出到底在给哪些项目花链路**——这是“先可观测再优化”的前提。教训：① **“谁在抢稀缺资源”必须能观测**，否则优化只能猜；本轮三个抱怨中有两个的真实原因（多键查找落空、占位符替换非空列表）都不是网速问题；② **并行后台工作必须有“焦点”概念**——“未暂停”不等于“用户在看”，默认可并行会把用户的等待时间拿去服务他不在看的对象；③ 同一实体被两条代码路径用两个不同 key 写入，就是定时炸弹，且症状只是“偶尔空白”，不是报错。
- **「远程终端」按钮必须无条件可用，且必须落在终端（2026-09-11）**：用户报“点左侧远程服务器的终端按钮，应该能直接进远程终端——有时就是需要进终端”。两个真原因，都是“设计没走完”而非缺少功能：① **按钮被禁用**——`disabled={status !== "connected" && status !== "connecting"}`，于是**从未探测过（disconnected）或上次失败（failed）的服务器根本点不动**（title 写着“连接后可用”），而连接设计里恰恰把“显式点终端”当作**唯一能手动输密码的落点**（`startPi:false` 的 ssh shell 就是登录输入面）——禁止这条路径等于禁止登录；② **复用逻辑会给出聊天标签**——`ensureRemoteConnection` 的候选是“同 remoteKey 的任意标签，优先 connection”，只有 pi 会话标签时回退 `candidates[0]`，而会话标签渲染的是 **ChatView**：用户点「终端」，界面给对话。三处修：① 抽纯函数 `pickServerTerminalTab(tabs, remoteKey)`（只认 `kind === "connection"` 且非 failed，ready 优先）并单测 6 例（含“同 remoteKey 的会话标签必须返回 null”的回归锁定）；② `handleOpenServerTerminal` 改走该函数、**先 `showTabImmediately` 上屏 + `tab:activate`，再 `waitConnState` 报真相**——需要密码的服务器是在**这个终端里**提示输入的，等 10s 状态超时才切过去，用户先看到的是“点了十秒没反应”（`startPi:false` 的标签 main 不会抢 activeId，必须显式 activate）；③ SidebarPane 去掉 `disabled`，title 分三态（已连接 / SSH 连接中 / 未连接或上次失败）。教训：① **“显式入口”与“隐式推导状态”冲突时，入口优先**——把“未连接就禁用”当成礼貌，实际是把唯一的凭据输入面锁在门外；状态点已经承担了“连没连”的表达，按钮该承担“我要干什么”；② **“复用同 key 的对象”必须校验对象的能力，而不是它的身份**——同一个 remoteKey 的标签可能渲染完全不同的视图，key 相等 ≠ 可替换；③ **先上屏再等结果**：任何“等握手/等就绪再切页面”的流程，都要先问“用户要等的时候，输入面在哪”。
